# AI 助手的项目全局规则

1. **演示文件位置**：每当用户要求创建、生成或编写“演示 (Demo)”文件、示例代码或演示时，这些文件的格式可能是html/vue等，您必须始终将这些文件保存并放置在 `Demos/` 的对应方案子目录中。
2. **多方案划分与防混淆规则**：
   - **方案一 (全貌方案)**：存放于 `Demos/01-方案一(全貌)/`
   - **方案二 (一期方案)**：存放于 `Demos/02-方案二(一期)/`
   - **防混淆操作要求**：AI 在读取、修改或新建文件前，必须校验文件所属的完整路径。不得跨方案目录混淆修改同名文件。若用户指令未明确说明方案版本，需先向用户确认或根据文件路径明确归属。
3. **目录创建**：如果 `Demos/` 下的方案文件夹不存在，请在保存文件之前创建它。
4. **保持根目录整洁**：除非用户明确要求，否则不要在根目录或其他目录中输出演示脚本或文件。
5. **样式快修规则**：当用户在指令中提及“样式快修”时，仅对页面代码（HTML/CSS/JS）进行直接、局部的修改和美化，不要执行任何自动拉起浏览器进行自动化验证、拨测、或模拟点击的操作。

### 项目脚本工具使用规范
1. 本项目所有的自动化脚本、排障脚本、更新工具 (如 `.py`, `.js`, `.sh` 等) 以及相关的依赖配置文件（如 `package.json`）均统一存放于 `scripts/` 文件夹中，保持根目录整洁。
2. AI 如果需要调用一些工具脚本时，必须先去 `scripts/` 目录下查阅有没有现成的脚本，没有的话再进行新的创建。
3. 执行项目构建/更新操作或安装依赖时，请统一进入 `scripts/` 目录下执行（例如 `cd scripts && npm install` 或 `python3 scripts/update.py`）。
